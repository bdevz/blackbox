# Blackbox

AI-powered RFP proposal generation platform. Generates complete government contract proposals using orchestrated AI agents with shared context.

## Quick Start

```bash
cp .env.example .env
# Fill in API keys
docker compose up -d
```

API at http://localhost:8000/api/health
